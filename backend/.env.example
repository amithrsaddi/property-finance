PORT=3000
WEBCLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-production
JWT_TTL=7d
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.example.mongodb.net/property-finance?retryWrites=true&w=majority

# On Netlify, set MONGODB_URI, JWT_SECRET, and WEBCLIENT_ORIGIN (your Netlify site URL).
# JWT_TTL is optional.
